import type {ReactNode} from 'react'; import {useQuery} from '@tanstack/react-query'; import {FolderKanban,HelpCircle,Plus} from 'lucide-react'; import {Link} from 'react-router-dom'; import {projectsApi} from '../services/api';
export function Dashboard(){const {data,isLoading,isError}=useQuery({queryKey:['projects'],queryFn:()=>projectsApi.list().then(r=>r.data.data)});const projects=data?.items||[];return <><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-medium text-blue-600">Workspace overview</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Good morning, researcher.</h1><p className="mt-2 text-slate-500">Keep your research programs moving forward.</p></div><Link to="/projects" className="btn-primary flex items-center gap-2"><Plus size={17}/> New project</Link></div>{isError&&<div className="card mb-5 text-red-600">Unable to load projects.</div>}<div className="grid gap-4 md:grid-cols-2"><Stat icon={<FolderKanban/>} label="Total projects" value={isLoading?'—':data?.total||0}/><Stat icon={<HelpCircle/>} label="Research questions" value="—"/></div><section className="mt-8"><div className="mb-4 flex items-center justify-between"><h2 className="text-xl font-bold">Recent projects</h2><Link className="text-sm font-semibold text-blue-600" to="/projects">View all</Link></div>{isLoading?<div className="card">Loading projects…</div>:projects.length===0?<div className="card py-12 text-center text-slate-500">No projects yet. Create your first research program.</div>:<div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{projects.slice(0,6).map(p=><Link className="card hover:border-blue-300" to={`/projects/${p.id}`} key={p.id}><div className="mb-5 flex justify-between"><span className="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-semibold text-blue-700">{p.status}</span></div><h3 className="font-bold">{p.name}</h3><p className="mt-2 line-clamp-2 text-sm text-slate-500">{p.description||'No description provided.'}</p></Link>)}</div>}</section></>}
function Stat({icon,label,value}:{icon:ReactNode;label:string;value:ReactNode}){return <div className="card flex items-center gap-4"><div className="rounded-xl bg-blue-50 p-3 text-blue-600 dark:bg-blue-950/50">{icon}</div><div><p className="text-sm text-slate-500">{label}</p><p className="mt-1 text-2xl font-bold">{value}</p></div></div>}
