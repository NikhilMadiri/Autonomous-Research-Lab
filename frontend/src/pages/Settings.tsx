import { Check, Moon, ShieldCheck } from 'lucide-react';
import { useUIStore } from '../store/ui';
export function Settings() { const { dark, toggle } = useUIStore(); return <><div className="mb-8"><p className="text-sm font-bold text-indigo-600">Workspace preferences</p><h1 className="mt-1 text-3xl font-bold">Settings</h1><p className="mt-2 text-slate-500">Tune your research workspace to fit the way you work.</p></div><div className="grid max-w-3xl gap-5"><section className="card"><div className="flex items-start gap-4"><div className="rounded-xl bg-indigo-50 p-3 text-indigo-600"><Moon size={20} /></div><div className="flex-1"><h2 className="font-bold">Appearance</h2><p className="mt-1 text-sm text-slate-500">Choose how Research Lab looks on this device.</p></div><button className="btn-secondary" onClick={toggle}>{dark ? 'Use light mode' : 'Use dark mode'}</button></div></section><section className="card"><div className="flex items-start gap-4"><div className="rounded-xl bg-emerald-50 p-3 text-emerald-600"><ShieldCheck size={20} /></div><div><h2 className="font-bold">API connection</h2><p className="mt-1 text-sm text-slate-500">Your workspace uses the configured FastAPI service.</p><p className="mt-4 flex items-center gap-2 text-sm font-semibold text-emerald-600"><Check size={16} /> Connected through the app API client</p></div></div></section></div></>; }
